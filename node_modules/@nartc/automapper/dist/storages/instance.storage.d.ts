/**
 * Internal InstanceStorage class
 * @private
 */
import { Constructible } from '../types';
import { MapNestedStorage } from './base-storage';
declare class InstanceStorage extends MapNestedStorage<Constructible, number> {
    private recursiveCounts;
    constructor();
    getDepthAndCount(parent: Constructible, member: string): [number | undefined, number | undefined];
    setCount(parent: Constructible, member: string, count: number): void;
    resetCount(parent: Constructible, member: string): void;
    resetAllCount(model: Constructible): void;
    private getCount;
    dispose(): void;
}
export declare const instanceStorage: InstanceStorage;
export {};
