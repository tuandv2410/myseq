import { AutoMapper, Constructible, MappingProfile } from '@nartc/automapper';
import { Logger } from '@nestjs/common';
import { Reflector } from '@nestjs/core';
export declare class AutomapperExplorer {
    private readonly reflector;
    private readonly logger;
    private readonly mapperMap;
    private readonly profileMap;
    constructor(reflector: Reflector, logger: Logger, mapperMap: Map<string, AutoMapper>, profileMap: Map<Constructible<MappingProfile>, Constructible<MappingProfile>>);
    explore(): void;
    private exploreProfile;
}
